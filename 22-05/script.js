// confirm is True or false
// Prompt is to take the input from user 
// alert 

// Implicit / explicit type conversion 
// the data which automatically converted by the JS is Implicit
// the data which is converted by the user with some command is Explicit

// var num1 = Number(prompt("Enter Number 1"));
// var num2 = +prompt("Enter Number 2");
// var sum = num1 + num2;

// console.log(typeof num2);
// console.log("the sum is" + sum);

var m = confirm("Do you want to leave this page");
alert("The output is")

// var -> global scopic variable
// let -> local scopic varibale === written under {} (if value assigned once can be changed later)
// const -> local scopic constant variable === written under {}  (if value assigned once cannot be changed)

{
    let a = 45
}

console.log(a);